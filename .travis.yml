cache:
    directories:
    - $HOME/.m2
    - $HOME/.kobalt

language: java

jdk:
  - oraclejdk8

install: true

script: ./kobaltw --log 3 simpleJavaAndroid:assemble

# advancedJavaAndroid:assemble androidFlavors:assembleFreeDebug flavors:assemble mixed:assemble 

